{
  "private": true,
  "scripts": {
    "build": "npm run build --prefix leadgen",
    "dev": "npm run dev --prefix leadgen",
    "start": "npm start --prefix leadgen"
  },
  "workspaces": [
    "leadgen"
  ]
}
